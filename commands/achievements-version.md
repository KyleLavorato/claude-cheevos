Check which version of the Claude Code Achievement System is installed.

Use the Bash tool to run:

```bash
~/.claude/achievements/cheevos version
```

Display the result to the user as a single line, e.g. "Claude Cheevos v1.142 is installed."
If the output is "dev", tell the user they are running a custom local build.
